# Application

Application web privée. Documentation interne, non publique.

## Développement

```bash
npm install
npm run dev
```

Les variables d'environnement requises sont listées dans `.env.example`.
Ne jamais committer de secret réel : le fichier `.env` est ignoré par git.
